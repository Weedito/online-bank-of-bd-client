import React from 'react';
import DBCards from './DBCards/DBCards';

const AdDashboard = () => {
    return (
        <div>
            <DBCards/>
        </div>
    );
};

export default AdDashboard;